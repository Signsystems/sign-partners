extends ./shared/layout.pug

block head
  - script = process.env.NODE_ENV === 'production' ? 'app-min.js' : 'app.js'
  script(type='text/javascript' src=`/assets/${script}` defer)

block content
  +header('signs')
  .signs
